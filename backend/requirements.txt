fastapi
uvicorn[standard]
python-dotenv

# ── RAG Layer 1: Data Ingestion ───────────────────────────────────────────────
llama-index-core>=0.11.0
llama-index-readers-file>=0.2.0
pypdf>=4.0.0
python-docx>=1.1.0
python-pptx>=1.0.0
olefile>=0.47

# ── HTTP: downloading source files from Supabase signed URLs ──────────────────
httpx>=0.27.0

# ── OCR: images and scanned PDFs ──────────────────────────────────────────────
# Both are optional at runtime. Without GEMINI_API_KEY set, images are refused
# with a clear message and every text-bearing format still works.
# PyMuPDF renders scanned PDF pages to images; google-genai runs the OCR.
pymupdf>=1.24.0
google-genai>=1.0.0

# ── Tests ─────────────────────────────────────────────────────────────────────
pytest>=8.0.0
