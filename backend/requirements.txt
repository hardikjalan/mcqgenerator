annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.0
click==8.4.1
colorama==0.4.6
fastapi==0.138.0
h11==0.16.0
idna==3.18
lxml==6.1.1
packaging==26.2
pillow==12.2.0
pydantic==2.13.4
pydantic_core==2.46.4
PyMuPDF>=1.24.0
google-genai>=0.1.0
python-dotenv>=1.0.1
python-pptx==1.0.2
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.49.0
xlsxwriter==3.2.9
httpx==0.28.1
pytesseract>=0.3.10

# ── Document extraction (LlamaIndex) ──────────────────────────────────────────
# Readers replace the hand-rolled loaders/extractors. They return
# List[Document], which is also the entry point for the RAG pipeline.
#   PyMuPDFReader → pdf   (needs the [pymupdf] extra)
#   DocxReader    → docx  (uses docx2txt, NOT python-docx)
#   PptxReader    → pptx  (uses python-pptx, pinned above)
# Images have no reader: ImageReader's default path pulls torch +
# transformers + sentencepiece, so gemini_ocr handles them directly.
llama-index-core>=0.14.23
llama-index-readers-file[pymupdf]>=0.6.0
docx2txt>=0.8
