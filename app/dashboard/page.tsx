'use client'

export default function Dashboard() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <div className="text-center">
        <h1 className="text-3xl font-bold gradient-text mb-4" style={{ fontFamily: 'Space Grotesk, sans-serif' }}>
          Dashboard — Coming Soon
        </h1>
        <p style={{ color: 'var(--text-secondary)' }}>You are logged in. The full dashboard is being built.</p>
      </div>
    </div>
  )
}
